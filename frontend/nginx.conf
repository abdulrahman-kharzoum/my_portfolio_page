server {
    listen 80;
    server_name localhost;

    # Path to the static files from the build
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # This is the key part: try to find the file,
        # otherwise fall back to index.html
        try_files $uri $uri/ /index.html;
    }
}